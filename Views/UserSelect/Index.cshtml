@model IEnumerable<TalentHunt.Models.userselect>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.finalpay)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.productionevent.ename)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.user.fname)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.finalpay)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.productionevent.ename)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user.fname)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.usid }) |
                @Html.ActionLink("Details", "Details", new { id=item.usid }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.usid })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
