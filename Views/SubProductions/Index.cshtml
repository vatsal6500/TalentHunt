@model IEnumerable<TalentHunt.Models.subproduction>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.startdate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.enddate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.plan.plantype)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.production.pname)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.startdate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.enddate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.plan.plantype)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.production.pname)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.spid }) |
                @Html.ActionLink("Details", "Details", new { id=item.spid }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.spid })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
