@model IEnumerable<TalentHunt.Models.eventrequire>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.agerange)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.gender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.payrange)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.production.pname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.productionevent.ename)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.talent.ttype)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.agerange)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.gender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.payrange)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.production.pname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.productionevent.ename)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.talent.ttype)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.erid }) |
                @Html.ActionLink("Details", "Details", new { id=item.erid }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.erid })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
